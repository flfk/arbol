$(document).ready(function() {
  // Populate stats
  var tree_count = 400;
  $('#treeCount').prepend(tree_count);
  var pages_left = 48;
  $('#pagesLeft').prepend(pages_left);
  pages_left--;

  /*
  // Snooze functionality
  $('.cross').hover(
    function(){
      //mouseEnterFunction
    }
    function(){
      //mouseLeaveFunction
    }
  );
  $('.cross').on('click', function(){
    //$('.bottomBar').fadeTo('fast', 1);
  });
  */
});
